services:
  - type: web
    name: cowsay-express-app
    env: node
    buildCommand: |
      apt-get update && apt-get install -y cowsay
      mkdir -p /usr/share/cows/ && cp custom_animal.cow /usr/share/cows/
      npm install
    startCommand: npm start
